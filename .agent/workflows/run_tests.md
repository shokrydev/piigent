---
description: Run tests using pytest from the virtual environment
---

1. Always use `.venv/bin/pytest` or `.venv/bin/python -m pytest`.
   `Run: .venv/bin/pytest [test_path] [args]`

// turbo
2. If running specific tests, use `-k "keyword"` or path selection.
